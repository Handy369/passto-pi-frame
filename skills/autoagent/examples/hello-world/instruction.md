# Hello World 任务

## 目标
创建一个文件 `/task/hello.txt`，包含以下内容：
```
Hello, World!
```

## 要求
1. 文件必须位于 `/task/hello.txt`
2. 内容必须完全匹配 "Hello, World!"（无引号）
3. 不要添加额外的空格或换行符

## 提示
- 使用 shell 命令创建文件：`echo "Hello, World!" > /task/hello.txt`
- 或使用 `printf` 命令
