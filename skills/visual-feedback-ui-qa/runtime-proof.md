# Visual Feedback UI QA Runtime Proof

此文件为外部验证资产。

用于在 `visual-feedback-ui-qa` 被创建或调整后，由外部验证机制检查：
- 是否优先要求真实运行态证据
- 是否避免仅凭代码/DOM 判定通过
- 是否输出结构化 findings（严重度 / surface / 证据 / 影响 / 修复建议）
- 是否没有误吸纯实现、纯定义、纯性能调试请求

它不属于 skill 运行时读取面，也不由 `SKILL.md` 负责维护。
